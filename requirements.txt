# requirements.txt - 伊利奶牛选配系统依赖清单
# 版本: 1.0.6
# 更新日期: 2025-09-19

# GUI框架 - 现代化用户界面
PyQt6>=6.4.0

# 数据库连接 - 阿里云PolarDB支持
sqlalchemy>=2.0.0  # 恢复sqlalchemy依赖以支持现有代码
pymysql>=1.0.0  # 直接使用PyMySQL操作数据库

# 安全加密 - 配置信息加密存储
cryptography>=41.0.0

# 数据处理核心 - 遗传数据分析
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
xlrd>=2.0.0
xlsxwriter>=3.1.0

# 报告生成 - PPT自动生成
python-pptx>=0.6.21

# 数据可视化 - 图表和血缘图
matplotlib>=3.7.0
seaborn>=0.12.0

# 网络图形分析 - 血缘关系图
networkx>=3.0.0

# 机器学习 - 遗传评估算法
scikit-learn>=1.3.0

# 计算机视觉 - 启动画面视频播放
opencv-python>=4.8.0

# 科学计算 - 遗传学统计分析
scipy>=1.10.0

# HTTP请求 - 版本检查和云端API
requests>=2.31.0

# API服务框架 - 认证API服务器
fastapi>=0.104.0
uvicorn>=0.24.0

# JWT令牌处理 - 用户认证
PyJWT>=2.8.0

# 安全密钥存储 - 跨平台密钥环
keyring>=24.0.0

# 系统信息获取 - 智能更新路径检测
psutil>=5.9.0

# 图像处理 - 界面图标和图表
Pillow>=10.0.0

# 并行处理 - 大数据集优化
joblib>=1.3.0

# 日期时间处理 - 配种记录分析
python-dateutil>=2.8.0

# Windows特定依赖
pywin32>=306; sys_platform == "win32"

# macOS特定依赖  
pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"

# 开发和构建工具（可选）
# PyInstaller>=5.13.0  # 程序打包
# black>=23.0.0        # 代码格式化
# pytest>=7.0.0       # 单元测试
# pre-commit>=3.0.0    # Git钩子

# 兼容性说明：
# - Python 3.9+ 推荐
# - 支持 Windows 10/11, macOS 10.14+
# - 如果在macOS上安装PyQt6遇到问题：
#   brew install qt@6
#   export PATH="/usr/local/opt/qt@6/bin:$PATH"
#
# 安装方法：
# pip install -r requirements.txt
